unsigned int store value that can be positive only
signed integer can be both positive and negative and it's range is half of what unsigned can store
bool = -1 < sizeof(int);//false because when we want to convert -1 into a unsigend integer the value will be huge.